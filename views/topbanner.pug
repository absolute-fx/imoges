section.page-header.imoges-grey.page-header-md
    .container
        h1 CONSTRUCTION&nbsp;
            span IMOGES
        ol.breadcrumb
            if breadcrumb
                each b in breadcrumb
                    if b.link
                        li
                            a(href=`${ b.link }`) #{ b.label }
                    else
                        li.active #{ b.label }